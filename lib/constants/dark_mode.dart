
import 'package:flutter/material.dart';
import 'colors.dart';

  // dark mode
ThemeData darkMode = ThemeData(
  colorScheme: ColorScheme.dark(
    background: backgroundDark,
    primary: dark,
    secondary: musicPlayerButton,
    surface: light
  )
);